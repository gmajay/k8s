apiVersion: v1
kind: Pod
metadata:
        name: sample-jenkins
spec:
   hostNetwork: true
   containers:
           - name: jenkins
             image: jenkins
             ports:
             - containerPort: 8080        

